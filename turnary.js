// Turnary operation 

let price = 500;
const isLeader = false;

// update constant variable turnary operator use
price = isLeader === true ? 0 : price + 100; 
// console.log("New price update :" ,price);
